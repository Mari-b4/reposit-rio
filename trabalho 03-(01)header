#ifndef HASH_ENCADEADA_H_INCLUDED
#define HASH_ENCADEADA_H_INCLUDED

typedef struct tipo_tabela tabela;
void inserir(tabela *T, char*);
tabela *inicializar_tabela(int);
int buscar(tabela *T, char*);
void remover(tabela *T, char*);
void mostrar(tabela *T);

#endif // HASH_ENCADEADA_H_INCLUDED
